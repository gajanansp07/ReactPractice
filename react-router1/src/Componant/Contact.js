import React from "react";

const Contact=()=>{
    return(<div>
        <h4>React Router</h4>
        <p>Routing is a process in which a user is directed to different pages bassed on there action or request.</p>
        <p>ReactJs Router is mainly used for developing Single Page Web Applications.</p>
        <p>React Router is used to define multiple routes in the application.</p>\
        <p>when user types a specific URL into the browser, and if this URL path matches any 'route' inside the router file,
            the user will be redirected to that particular route. </p>
        <p>React Router is a standard library system built on top of the React and usedto create routing in the React application using React Router Package</p>
    </div>);
}
export default Contact;